# LogApiReflection

## Objective:
The objective of this api is to apply the concept of reflection by building an api with log.

## How to execute:
After downloading or cloning the project, execute the following commands:
- dotnet build
- dotnet run

By default, the application will be executed at the following address:
https://localhost:5001
